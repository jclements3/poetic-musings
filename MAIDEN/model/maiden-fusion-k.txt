; -*- mode: idef0 -*-
; GENERATED by scripts/py2idef0.py -- do not edit; rerun:
;   scripts/py2idef0.py K 'Fusion And Camera Tracking' <the same .py files>
tK Fusion And Camera Tracking
## Derived from the Python sources via ast: boxes are the
## cross-module call graph, i ports are parameters, o ports
## are return annotations (missing annotation = missing o,
## on purpose). Docstring first lines carry the SYS/D refs.
  a# Fuse Session
  ## Ingest every station file in a session directory and fuse.
    i# Session Dir
    i# Epoch Hz
    a# Load
    ## Stream StateSamples in file order; constant memory.
      i# Path
      o# StateSample
      a# Validate
      ## Raise ValueError on any IF-4 violation. Cheap; call it in adapters.
        i# S
  a# Run Session
  ## maiden.runner — `maiden run --session DIR` (D9 §Session workflow §6).
    i# Session Dir
    i# Out Dir
    i# Field Cfg
    o# Dict
    a# Fuse
    ## Full scheduler per lesson 13: sequential predict-to-measurement
      i# Samples
      i# Stations
      i# Epoch Hz
      o# FuseStats
      o# StateSample
    a# Poses From Session
    ## Station Poses from the session's own TMATS (works for twin or real).
      i# Session Dir
      o# Pose
      a# Enu From Lla
      ## Exact ENU meters of `lla` relative to `origin_lla`.
        i# Origin Lla
        i# Lla
        o# Ndarray
      a# Describe
      ## Parse only the first packet (TMATS, per IF-1) into a descriptor.
        i# Path
        o# Station
        o# Aircraft
    a# Load
    ## Stream StateSamples in file order; constant memory.
      i# Path
      o# StateSample
      a# Validate
      ## Raise ValueError on any IF-4 violation. Cheap; call it in adapters.
        i# S
    a# Render
    ## One Letter page + sidecar + clips. Returns paths.
      i# Fd
      i# Out Dir
      o# Dict
    a# Score Sequence
    ## Score every recognized maneuver in schedule order (VT-20 shape).
      i# Events
      i# Samples
      i# Calibration
      i# Rubric
      o# ManeuverScore
