# Real config-management target: this host, managing the actual
# observability docker-compose stack (app/status-service + Prometheus +
# Grafana). See ansible/README.md for why localhost, and the honest
# limits of that (no fleet, no SSH-based remote hosts).
[observability]
localhost ansible_connection=local
